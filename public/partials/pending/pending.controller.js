'use strict';
(function(){
	angular
		.module("spotifyApp")
		.controller("PendingCtrl", PendingCtrl);

		PendingCtrl.$inject = ['$scope', '$http'];

		function PendingCtrl ($scope, $http) {
			
		}
})();
